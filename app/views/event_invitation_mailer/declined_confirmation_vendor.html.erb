<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
      background-color: #f5f5f5;
      padding: 20px;
      margin: 0;
      line-height: 1.6;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      padding: 30px;
      border: 1px solid #e0e0e0;
    }
    .header {
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 1px solid #e0e0e0;
    }
    .header strong {
      font-size: 18px;
      color: #1a1a1a;
    }
    .title {
      font-size: 22px;
      font-weight: 600;
      color: #1a1a1a;
      margin: 0 0 15px 0;
    }
    .content {
      color: #333333;
      font-size: 15px;
    }
    .content p {
      margin: 0 0 15px 0;
    }
    .event-details {
      background-color: #f9f9f9;
      padding: 15px;
      margin: 15px 0;
      border: 1px solid #e0e0e0;
    }
    .event-details p {
      margin: 5px 0;
      font-size: 14px;
      color: #333333;
    }
    .footer {
      margin-top: 25px;
      padding-top: 20px;
      border-top: 1px solid #e0e0e0;
      font-size: 13px;
      color: #888888;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <strong><%= @organization.name %></strong>
    </div>

    <h1 class="title">Invitation Declined</h1>

    <div class="content">
      <p>Hi <%= @vendor_contact.name %>,</p>

      <p>We have received your response declining the invitation to participate in <strong><%= @event.title %></strong>.</p>

      <div class="event-details">
        <p><strong>Event:</strong> <%= @event.title %></p>
        <p><strong>Date:</strong> <%= @event.event_date.strftime("%B %d, %Y at %I:%M %p") %></p>
        <% if @event.location.present? %>
          <p><strong>Location:</strong> <%= @event.location %></p>
        <% end %>
      </div>

      <% if @invitation.response_notes.present? %>
        <p><strong>Your note:</strong> "<%= @invitation.response_notes %>"</p>
      <% end %>

      <p>We understand and appreciate you taking the time to respond. We hope to have the opportunity to work with you in the future.</p>

      <p>If you have any questions or would like to discuss future opportunities, please contact <%= @organization.name %>.</p>
    </div>

    <div class="footer">
      <p style="margin: 0 0 12px 0;">Confirmation sent via Voxxy Presents</p>
      <p style="margin: 0; font-size: 12px; color: #aaaaaa;">Powered by Voxxy Presents</p>
    </div>
  </div>
</body>
</html>
