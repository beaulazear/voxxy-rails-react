<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background-color: #f4f4f4;
      padding: 20px;
      margin: 0;
      line-height: 1.6;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background: #fff;
      padding: 40px;
      border-radius: 8px;
    }
    .content {
      color: #333;
      font-size: 16px;
    }
    .content p {
      margin: 0 0 20px 0;
    }
    .event-details {
      margin: 30px 0;
      padding: 0;
    }
    .event-details p {
      margin: 12px 0;
      font-size: 16px;
    }
    .button-container {
      text-align: center;
      margin: 40px 0 30px 0;
    }
    .button {
      display: inline-block;
      padding: 14px 32px;
      background-color: #9333ea;
      color: #fff;
      text-decoration: none;
      font-size: 16px;
      font-weight: 600;
      border-radius: 8px;
      transition: background-color 0.2s;
    }
    .button:hover {
      background-color: #7e22ce;
    }
    .footer {
      margin-top: 40px;
      padding-top: 20px;
      border-top: 1px solid #e5e7eb;
      text-align: center;
      color: #6b7280;
      font-size: 14px;
    }
    .footer a {
      color: #9333ea;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <p>Hi <%= @vendor_contact.business_name.present? ? @vendor_contact.business_name : @vendor_contact.name %>,</p>

      <p>We're excited to announce <strong><%= @event.title %></strong> and would love to have you join us!</p>

      <div class="event-details">
        <% if @event.event_date.present? %>
          <p>📅 <strong>Event Date:</strong> <%= @event.event_date.strftime("%B %d, %Y") %></p>
        <% end %>

        <% if @event.venue.present? || @event.location.present? %>
          <p>📍 <strong>Location:</strong>
            <%= [@event.venue, @event.location].compact.join(', ') %>
          </p>
        <% end %>

        <% if @event.application_deadline.present? %>
          <p>⏰ <strong>Application Deadline:</strong> <%= @event.application_deadline.strftime("%B %d, %Y") %></p>
        <% end %>
      </div>

      <p>This is a great opportunity to showcase your work and connect with the community.</p>

      <p>Best,<br><%= @organization.name %></p>

      <div class="button-container">
        <a href="<%= @invitation_url %>" class="button">Apply Now</a>
      </div>
    </div>

    <div class="footer">
      <p><a href="#">Unsubscribe</a> - <a href="#">Unsubscribe Preferences</a></p>
    </div>
  </div>
</body>
</html>
