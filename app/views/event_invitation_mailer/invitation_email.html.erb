<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
      background-color: #f5f5f5;
      padding: 20px;
      margin: 0;
      line-height: 1.6;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      padding: 30px;
      border: 1px solid #e0e0e0;
    }
    .header {
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 1px solid #e0e0e0;
    }
    .header strong {
      font-size: 18px;
      color: #1a1a1a;
    }
    .content {
      color: #333333;
      font-size: 15px;
    }
    .content p {
      margin: 0 0 15px 0;
    }
    .event-details {
      background-color: #f9f9f9;
      padding: 15px;
      margin: 15px 0;
      border: 1px solid #e0e0e0;
    }
    .event-details p {
      margin: 5px 0;
      font-size: 14px;
      color: #333333;
    }
    .link {
      color: #0066cc;
      text-decoration: underline;
    }
    .footer {
      margin-top: 25px;
      padding-top: 20px;
      border-top: 1px solid #e0e0e0;
      font-size: 13px;
      color: #888888;
    }
    .footer a {
      color: #0066cc;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <strong><%= @organization.name %></strong>
    </div>

    <div class="content">
      <p>Hi <%= @vendor_contact.business_name.present? ? @vendor_contact.business_name : @vendor_contact.name %>,</p>

      <p>We are excited to announce <strong><%= @event.title %></strong> and would like to invite you to participate.</p>

      <div class="event-details">
        <% if @event.event_date.present? %>
          <p><strong>Event Date:</strong> <%= @event.event_date.strftime("%B %d, %Y") %></p>
        <% end %>

        <% if @event.venue.present? || @event.location.present? %>
          <p><strong>Location:</strong>
            <%= [@event.venue, @event.location].compact.join(', ') %>
          </p>
        <% end %>

        <% if @event.application_deadline.present? %>
          <p><strong>Application Deadline:</strong> <%= @event.application_deadline.strftime("%B %d, %Y") %></p>
        <% end %>
      </div>

      <p>This is an opportunity to showcase your work and connect with the community.</p>

      <p>Apply here: <a href="<%= @invitation_url %>" class="link"><%= @invitation_url %></a></p>

      <p>Best regards,<br/><%= @organization.name %></p>
    </div>

    <div class="footer">
      <p style="margin: 0 0 12px 0;">If you didn't expect this email, you can safely ignore it.</p>
      <p style="margin: 0 0 12px 0; font-size: 12px; color: #888888;">
        <a href="<%= @unsubscribe_url %>" style="color: #888888; text-decoration: underline;">Unsubscribe from these emails</a>
      </p>
      <p style="margin: 0; font-size: 12px; color: #aaaaaa;">Powered by Voxxy Presents</p>
    </div>
  </div>
</body>
</html>
