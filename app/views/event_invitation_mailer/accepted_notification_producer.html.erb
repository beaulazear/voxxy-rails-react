<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
      background-color: #f5f5f5;
      padding: 20px;
      margin: 0;
      line-height: 1.6;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      padding: 30px;
      border: 1px solid #e0e0e0;
    }
    .header {
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 1px solid #e0e0e0;
    }
    .header strong {
      font-size: 18px;
      color: #1a1a1a;
    }
    .title {
      font-size: 22px;
      font-weight: 600;
      color: #1a1a1a;
      margin: 0 0 15px 0;
    }
    .content {
      color: #333333;
      font-size: 15px;
    }
    .content p {
      margin: 0 0 15px 0;
    }
    .details-box {
      background-color: #f9f9f9;
      padding: 15px;
      margin: 15px 0;
      border: 1px solid #e0e0e0;
    }
    .details-box p {
      margin: 5px 0;
      font-size: 14px;
      color: #333333;
    }
    .footer {
      margin-top: 25px;
      padding-top: 20px;
      border-top: 1px solid #e0e0e0;
      font-size: 13px;
      color: #888888;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <strong>Voxxy Presents</strong>
    </div>

    <h1 class="title">Invitation Accepted</h1>

    <div class="content">
      <p>Hello,</p>

      <p><strong><%= @vendor_contact.name %></strong> (<%= @vendor_contact.try(:business_name) || @vendor_contact.email %>) has accepted your invitation to participate in <strong><%= @event.title %></strong>.</p>

      <div class="details-box">
        <p><strong>Vendor Contact Details:</strong></p>
        <p><strong>Name:</strong> <%= @vendor_contact.name %></p>
        <% if @vendor_contact.try(:business_name).present? %>
          <p><strong>Company:</strong> <%= @vendor_contact.business_name %></p>
        <% end %>
        <p><strong>Email:</strong> <%= @vendor_contact.email %></p>
        <% if @vendor_contact.phone.present? %>
          <p><strong>Phone:</strong> <%= @vendor_contact.phone %></p>
        <% end %>
        <% if @invitation.response_notes.present? %>
          <p><strong>Their message:</strong> "<%= @invitation.response_notes %>"</p>
        <% end %>
      </div>

      <p>You can now follow up with <%= @vendor_contact.name %> to coordinate the details of their participation.</p>
    </div>

    <div class="footer">
      <p style="margin: 0 0 12px 0;">Notification sent via Voxxy Presents</p>
      <p style="margin: 0; font-size: 12px; color: #aaaaaa;">Powered by Voxxy Presents</p>
    </div>
  </div>
</body>
</html>
